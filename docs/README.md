# 演示资产

| 文件 | 内容 | 状态 |
|---|---|---|
| `screenshot.png` | 真机截图：工作区 16 篇文档的列表 + 就地预览，顶行显示当前排序依据 | ✅ 已就位 |
| `demo-reorder.gif` | 排序跟着对话走（对话切到「竞品」→ 列表 1 秒内重排） | ⬜ 待补 |

## 当前这张截图覆盖了什么、没覆盖什么

**覆盖了**：插件真能跑、面板长什么样、列表 + 就地预览、排序依据那行是怎么写的。

**没覆盖**：**「排序跟着对话走」这条唯一差异化的画面** —— 图里是新会话的兜底态
（按工作区名退化排序）。

按发布规划 §4，最理想的第 1 屏是那个重排的动图。补录要求：

- 先聊一句（例如「帮我看看竞品那几家怎么样」），等列表重排、顶行换成按关键词排序，再截/录
- 只截「对话 + 右侧栏」约 400px，不要拍全屏，字要看得清
- 深色主题，动图 6–8 秒、循环播放能无缝衔接
- **必须真机，不要拿 HTML 原型充数** —— 真机能录出来本身就是「这东西真能跑」的证明

## 换图时要同步改的地方

1. 把文件放进本目录
2. 改 `README.md` / `README.en.md` 第 1 屏的 `<img src=...>`（用绝对 raw URL，npm 页也能渲染）
3. **同步改两处图注** —— 图注必须描述图里真实显示的状态，不要把兜底态说成相关性排序
   （市场规则把描述当作对插件的声明，会与代码核对）

